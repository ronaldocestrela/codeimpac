# AGENTS.md
## Full Instructions for AI Agent Implementation

# Project Overview
This project is a SaaS platform that:
- Connects to GitHub
- Analyzes developer contributions (commits and pull requests)
- Identifies approved pull requests using GitHub review states
- Uses LLM to generate professional executive reports for leadership
- Operates with subscription-based billing and entitlement control

# Product Premise (Non-Negotiable)
- Developers authenticate with GitHub
- Developers select one or more repositories to analyze
- The system ingests commits and pull requests for the selected repositories
- The system classifies PR approval status based on GitHub review data
- The system generates a leadership-ready report with evidence and metrics
- The platform must support paid plans with clear usage limits and entitlement enforcement
- Billing events (subscription, invoice, payment status) must be traceable and auditable
- A backoffice panel must exist for internal operational support and customer care

# Tech Stack

## Backend
- .NET 10 Web API
- SQL Server
- Entity Framework Core
- Clean Architecture
- Hangfire (background jobs)

## Frontend
- React (Vite + TypeScript)
- Client-side rendering
- Axios for API
- React Query for data fetching
- TailwindCSS for UI

---

# Frontend Architecture

## Structure
```
src/
 ├── app/
 ├── components/
 ├── pages/
 ├── hooks/
 ├── services/
 ├── store/
 ├── types/
 ├── utils/
 └── routes/
```

## Core Concepts

### State Management
- Use React Query for server state
- Use Zustand or Context API for client state

### API Layer
All API calls must go through:
```
services/api.ts
```

### Authentication
- JWT stored in memory + localStorage fallback
- Interceptor for token injection

---

# Backend Rules

- Follow Clean Architecture strictly
- No business logic in controllers
- Use MediatR pattern (commands/queries)
- LLM must be abstracted via interface

---

# AI Integration Rules

- Never call LLM directly in controller
- Use IAIOrchestrator
- Always log prompt and response
- Use retry policies
- Build prompts with structured contribution inputs (commits + approved PRs)
- Require deterministic preprocessing before LLM (filtering, grouping, metrics)
- Always keep traceable evidence IDs in generated insights

# Contribution Analysis Rules

- Commits and pull requests are first-class entities in the domain model
- Approved PR classification must use GitHub review state (`APPROVED`) and be persisted
- PR analysis must include status dimensions: open, closed, merged, approved
- Keep author, repository, timestamps, reviewers, and links for auditability

# Executive Report Requirements

- Reports must target managers/supervisors (clear, concise, outcome-oriented)
- Include period, repositories analyzed, and developer scope
- Include objective metrics (volume, throughput, review outcomes, cycle indicators)
- Include qualitative highlights generated by LLM with supporting evidence
- Include risks/blocked items and recommended next actions

# Backoffice Requirements

- Provide admin-only access with role-based authorization
- Manage users, account status, and support history
- Monitor and operate background jobs (sync, AI summary, report generation)
- Allow support actions with full audit trail (retry, revoke token access, force re-sync)
- Expose operational dashboards (job health, ingestion failures, billing alerts)

# Billing & Subscription Requirements

- Support plan catalog with entitlements per user (repositories, report volume, retention)
- Integrate a payment provider for checkout, payment method management, and invoices
- Persist billing entities and lifecycle states (trial, active, past_due, canceled)
- Process provider webhooks with idempotency and retry safety
- Enforce plan limits consistently in backend services and frontend UX
- Keep billing events linked to user context for traceability

---

# Coding Standards

- Use SOLID principles
- Use async/await everywhere
- Always validate input
- Use DTOs for API communication
- Never expose domain entities directly

---

# Testing

- Unit tests for Application layer
- Integration tests for API
- Mock external services (GitHub, LLM)
- Add tests for approved PR classification rules
- Add tests for commit ingestion and repository filtering
- Add tests validating report sections and evidence mapping
- Add tests for RBAC and admin action authorization in backoffice endpoints
- Add tests for billing webhook idempotency and subscription state transitions
- Add tests for entitlement enforcement (plan limits) in API and UI flows

---

# Definition of Done

A feature is complete when:
- API endpoint works
- Frontend integrated
- LLM output validated
- Logs are stored
- Tests pass
- Commits and approved PRs are traceable from source to report
- Report is readable by leadership and includes objective evidence
- Backoffice operations are auditable and role-protected
- Billing/subscription flow is operational end-to-end with webhook reliability
- TDD
- Always update documentation
